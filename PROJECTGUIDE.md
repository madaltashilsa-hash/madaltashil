# الدليل المرجعي — موقع مدى التسهيل للتجارة

> **هذا الملف هو المرجع الوحيد للمشروع.** اقرأه قبل أي عمل جديد، وحدّثه بعد أي تعديل.
> آخر تحديث: أغسطس 2026

---

## المحتويات

1. [نظرة عامة](#1-نظرة-عامة)
2. [البنية التقنية](#2-البنية-التقنية)
3. [خريطة الملفات](#3-خريطة-الملفات)
4. [ترتيب الصفحة الرئيسية](#4-ترتيب-الصفحة-الرئيسية)
5. [نظام التصميم](#5-نظام-التصميم)
6. [مشغّل الفيديوهات](#6-مشغّل-الفيديوهات)
7. [سلايدر الصور](#7-سلايدر-الصور)
8. [مسارات واتساب والنماذج](#8-مسارات-واتساب-والنماذج)
9. [نظام الأيقونات](#9-نظام-الأيقونات)
10. [الصور: التحويل والتسمية](#10-الصور-التحويل-والتسمية)
11. [النطاق والاستضافة](#11-النطاق-والاستضافة)
12. [كيف تنشر تعديلًا](#12-كيف-تنشر-تعديلًا)
13. [قائمة فحص قبل النشر](#13-قائمة-فحص-قبل-النشر)
14. [حل المشكلات الشائعة](#14-حل-المشكلات-الشائعة)
15. [قواعد ومحاذير](#15-قواعد-ومحاذير)
16. [بيانات ثابتة](#16-بيانات-ثابتة)

---

## 1. نظرة عامة

| البند | القيمة |
|---|---|
| اسم الشركة | شركة مدى التسهيل للتجارة |
| الشعار النصي | تسهيل اليوم .. نجاح الغد |
| النشاط | توفير أحدث الأجهزة التقنية بخيارات تقسيط ودفع لاحق |
| الموقع | `https://madaltashilsa.com` |
| المستودع | `github.com/madaltashilsa-hash/madaltashil` |
| الاستضافة | GitHub Pages (مجانية) |
| اللغة | عربية · اتجاه RTL |
| الحجم الكلي | ~11 ميجابايت |

---

## 2. البنية التقنية

**الموقع ثابت بالكامل (Static Site)** — لا خادم، لا قاعدة بيانات، لا إطار عمل.
HTML + CSS + JavaScript خام فقط. هذا اختيار مقصود:

- سرعة تحميل عالية
- استضافة مجانية عبر GitHub Pages
- لا تكلفة صيانة أو خوادم
- أي تعديل = تعديل ملف نصي ورفعه

**لا يوجد:** npm · build step · React · jQuery · Bootstrap · أي مكتبة خارجية.
الخط الوحيد المُحمَّل خارجيًا هو **Tajawal** من Google Fonts.

**التوافق:** الكود مكتوب بـ ES5 (`var` لا `let`) لضمان العمل على المتصفحات القديمة.

---

## 3. خريطة الملفات

### صفحات HTML

| الملف | الوصف |
|---|---|
| `index.html` | الصفحة الرئيسية — كل المحتوى الأساسي |
| `about.html` | تعرف علينا: من نحن · رؤيتنا · رسالتنا · أهدافنا |
| `faq.html` | الأسئلة الشائعة الكاملة (16 سؤالًا) |
| `eligibility-info.html` | شروط الأهلية وخطوات التحقق |
| `brand-preview.html` | معاينة مفاهيم الشعار — صفحة داخلية غير مرتبطة بالقائمة |
| `intro-video.html` | ⚠️ **ملف قديم مهجور** — لا شيء يشير إليه، يمكن حذفه |

### ملفات CSS

| الملف | المسؤولية | الترتيب |
|---|---|---|
| `style.css` | الأساس: المتغيّرات · الهيدر · القائمة · الأزرار · النماذج · التذييل | 1 |
| `hero-slider.css` | سلايدر الصور | 2 |
| `pages.css` | الصفحات الداخلية | 3 |
| `mt-video.css` | مشغّل الفيديوهات | 4 |
| `mt-ui.css` | **تحسينات فوق كل ما سبق**: نافذة واتساب · تباعد النماذج · مسافات الأقسام · القائمة الجانبية | 5 — الأخير |

> **مهم:** `mt-ui.css` يجب أن يبقى **آخر** ملف مُحمَّل، لأنه يتجاوز قواعد الملفات السابقة.

### ملفات JavaScript

| الملف | المسؤولية | الأسطر |
|---|---|---|
| `app.js` | المحرّك الرئيسي: الأيقونات · القائمة · النوافذ · معالج الأهلية · النماذج · واتساب | 1076 |
| `hero-slider.js` | بناء سلايدرات الصور الأربعة | 214 |
| `mt-video.js` | بناء الفيديوهات الأربعة وتشغيلها | 289 |
| `mt-wa.js` | نافذة «جاري تحويلكم لرعاية خدمة العملاء» بعدّاد 5 ثوانٍ | 115 |
| `pages.js` | منطق الصفحات الداخلية | 76 |

**ترتيب التحميل في `index.html`:**
```html
<script src="mt-wa.js" defer></script>
<script src="mt-video.js" defer></script>
<script src="hero-slider.js"></script>
<script src="app.js"></script>
<script src="pages.js"></script>
```

### المجلدات

```
assets/
├── icon-white.png          الشعار الأبيض (مشاهد الفيديو)
├── logo.png                الشعار (نوافذ الإرسال)
├── logo-full-light.png     الشعار الكامل الفاتح (شاشة البداية)
├── logo-full-dark.png      الشعار الكامل الداكن (التذييل)
├── slides/                 28 ملف — 14 شريحة × نسختين (سطح مكتب + جوال)
└── video/                  56 ملف — 28 صورة × نسختين + README

build/
├── convert-banners.py          محوّل شرائح السلايدر
├── convert-video-images.py     محوّل صور الفيديو
├── gen-pages.py
├── render.mjs
└── art.html
```

### ملفات التوثيق

| الملف | المحتوى |
|---|---|
| `PROJECT-GUIDE.md` | **هذا الملف** — المرجع الشامل |
| `README.md` | نظرة سريعة على المستودع |
| `SLIDER-README.md` | تفاصيل السلايدر وترتيب الصفحة |
| `assets/video/README.md` | فهرس صور الفيديو وتوزيعها |
| `CNAME` | يحتوي سطرًا واحدًا: `madaltashilsa.com` — **لا تحذفه** |

---

## 4. ترتيب الصفحة الرئيسية

```
Header (ثابت أعلى الشاشة)
│
├─  1) video-brand        فيديو 1: العلامة              [mtv-tint]
├─  2) سلايدر A            4 شرائح
├─  3) eligibility        تحقق من أهليتك (يُحقن بـ JS)
├─  4) home               نقرّب لك أحدث التقنية
├─  5) video-phones       فيديو 2: الجوالات
├─  6) faq                الأسئلة الشائعة (4 أسئلة + رابط)
├─  7) سلايدر C            4 شرائح
├─  8) about              تعرف علينا
├─  9) video-tablets      فيديو 3: الأجهزة اللوحية       [mtv-tint]
├─ 10) سلايدر B            3 شرائح
├─ 11) google-rating      آراء العملاء
├─ 12) سلايدر D            3 شرائح
└─ 13) video-wearables    فيديو 4: الساعات والصوتيات
│
Footer (أوقات العمل · العنوان الوطني · التواصل)
```

**ملاحظات على الترتيب:**

- الفيديوهات في المواضع **1 · 5 · 9 · 13** — فواصل متساوية عبر الصفحة
- قسم `eligibility` **يُحقن بـ JavaScript** عبر `injectEligibilitySection()` في `app.js`،
  ويُوضع بعد العنصر `<div id="elig-anchor">` مباشرة.
  **لنقل قسم الأهلية: انقل `elig-anchor` في `index.html`، لا تعدّل الـ JS.**
- الأصناف `mtv-tint` تعطي خلفية زرقاء فاتحة للفيديو — تُستخدم بالتناوب لكسر الرتابة

---

## 5. نظام التصميم

### الألوان — معرّفة في `style.css` تحت `:root`

```css
--navy:      #0B1C33   /* الأزرق الداكن الأساسي */
--navy-2:    #122A4D
--navy-3:    #1C3A63
--gold:      #2F6FED   /* اللون المميّز (أزرق رغم الاسم) */
--gold-light:#7FB2FF
--off-white: #F3F7FC   /* خلفية الصفحة */
--ink:       #0E1E36   /* لون النص */
--ink-soft:  #4B5A72   /* نص ثانوي */
--line:      #E6E9EF   /* الحدود */
```

> **تنبيه:** المتغيّر اسمه `--gold` لأسباب تاريخية لكن قيمته **أزرق**. لا تغيّر الاسم — مستخدم في عشرات المواضع.

### ألوان مشغّل الفيديو — في `mt-video.css`

```css
--mtv-navy: #0B1C33
--mtv-deep: #071527
--mtv-blue: #1D6FE0
--mtv-cyan: #26C6F5
```

### الظلال والانحناءات

```css
--radius-lg: 22px    --radius-md: 16px    --radius-sm: 10px
--shadow-soft:   0 12px 32px rgba(11,28,51,.10)
--shadow-strong: 0 20px 50px rgba(11,28,51,.18)
--shadow-gold:   0 8px 24px rgba(47,111,237,.35)
```

### الخط

`Tajawal` بأوزان 400 · 500 · 700 · 800 — يُحمَّل من Google Fonts.
الاحتياطي: `Tahoma, Arial, sans-serif`.

### نقاط التوقّف (Breakpoints)

| المدى | الاستهداف |
|---|---|
| `≤ 380px` | جوالات صغيرة |
| `≤ 480px` | جوالات |
| `≤ 768px` | جوال (النقطة الرئيسية) |
| `≤ 900px` | لوحي |
| `≥ 1440px` | شاشات كبيرة |

### المسافات

معرّفة في `mt-ui.css`:

| العنصر | سطح المكتب | جوال |
|---|---|---|
| الأقسام | 64–110 بكسل | 48–72 |
| حول السلايدر | 48–86 بكسل | 36–56 |
| قبل التذييل | 56–104 بكسل | 44–68 |
| أقسام الفيديو | 72–132 بكسل | 56–84 |

---

## 6. مشغّل الفيديوهات

الفيديوهات ليست ملفات `.mp4` — بل **عروض شرائح مبنية بـ HTML/CSS/JS**.
هذا يعني: حجم أصغر بكثير، تعديل فوري بلا برامج مونتاج، ونص حيّ قابل للفهرسة.

### المواصفات

| البند | القيمة |
|---|---|
| عدد الفيديوهات | 4 |
| مجموع الصور | 28 — لا تتكرر صورة بين فيديو وآخر |
| مدة المشهد | 8 ثوانٍ |
| الافتتاحية والختامية | 10 ثوانٍ لكل منهما (شعار + عبارة) |
| الإعادة | تلقائية بلا نهاية |
| موضع النص | شريط مستقل **أسفل** الصورة — لا يغطّيها إطلاقًا |
| الأداء | يتوقّف خارج الشاشة ويستأنف عند الظهور |

### تأثيرات الانتقال — خمسة تتناوب

| الاسم | الوصف |
|---|---|
| `zoom` | تقريب من 0.90 إلى 1 |
| `wipe` | مسح جانبي بـ `clip-path` |
| `blur` | خروج من ضبابية 22 بكسل |
| `slide` | انزلاق أفقي 9% |
| `rise` | صعود رأسي 7% |

جميعها تبدأ بمقياس **أصغر من 1** فلا يُقتطع أي جزء من الصورة.

**تأثيرات إضافية:** وميض ضوئي يمرّ فوق الصورة · دخول متدرّج للنص · نقطة نابضة · حلقتان تتوسّعان خلف الشعار · شريط تقدّم مقسّم قطعة لكل مشهد.

### التوزيع

| الفيديو | المفتاح | المشاهد | المحتوى |
|---|---|---|---|
| 1 | `brand` | 6 | العلامة والخدمات والتقسيط |
| 2 | `phones` | 7 | آيفون 17 بكل فئاته |
| 3 | `tablets` | 10 | تشكيلة آيباد الكاملة |
| 4 | `wearables` | 5 | Apple Watch و AirPods |

**قاعدة الترتيب — مهمة:** الصور مصنّفة إلى ثلاث عائلات تصميم:

| الرمز | العائلة | الوصف |
|---|---|---|
| ▪ | `spec` | بطاقة مواصفات منتج واحد |
| ● | `grid` | شبكة أو كولاج متعدد |
| ◆ | `banner` | لافتة عبارة تقسيط |

**لا تضع تصميمين من نفس العائلة متتاليين** — يبدو للزائر تكرارًا حتى لو كانت المنتجات مختلفة. التفاصيل الكاملة في `assets/video/README.md`.

### كيف تعدّل فيديو

كل شيء في مصفوفة `VIDEOS` أعلى `mt-video.js`:

```js
brand: {
  eyebrow: 'فيديو تعريفي',              // الشارة الصغيرة فوق العنوان
  title:   'مدى التسهيل للتجارة',        // عنوان القسم
  sub:     'تسهيل اليوم .. نجاح الغد',   // الوصف تحته
  intro:   'تسهيل اليوم .. نجاح الغد',   // عبارة مشهد الافتتاحية
  outro:   'نقرّب لك أحدث التقنية',      // عبارة مشهد الختامية
  scenes: [
    ['اسم-الصورة', 'النص الذي يظهر أسفل المشهد'],
    // …
  ]
}
```

**لإضافة مشهد:** أضف سطرًا في `scenes` — التأثير يُوزَّع تلقائيًا بالتناوب.
**لتغيير المدد:** عدّل `SCENE_MS` و `LOGO_MS` أعلى الملف.

### كيف تضيف فيديو خامسًا

1. أضف مفتاحًا جديدًا في `VIDEOS` داخل `mt-video.js`
2. أضف في `index.html` حيث تريده:
   ```html
   <section id="video-الاسم" class="mtv-section" data-mtv="المفتاح"></section>
   ```
3. أضف `mtv-tint` للصنف إن أردت خلفية ملوّنة

---

## 7. سلايدر الصور

14 تصميمًا موزّعة على 4 مجموعات، تُبنى من `hero-slider.js`:

| المجموعة | الشرائح | الموضع |
|---|---|---|
| A | a1 a2 a3 a4 | بعد الفيديو الأول |
| B | b1 b2 b3 | بعد فيديو الآيباد |
| C | c1 c2 c3 c4 | بعد الأسئلة الشائعة |
| D | d1 d2 d3 | بعد آراء العملاء |

**الإعدادات** أعلى `hero-slider.js`:
```js
var AUTOPLAY = 6200;   // مدة عرض الشريحة (ملّي ثانية)
var SWAP = 900;        // مدة الانتقال — يجب أن تطابق CSS
```

**الاستدعاء في HTML:**
```html
<div class="mt-slider" data-slider="a"></div>
<div class="mt-slider mt-slider-roomy" data-slider="a"></div>  <!-- بمسافات أوسع -->
```

كل شريحة لها `ar` (نسبة العرض للارتفاع) في `GROUPS` — لا تنسَ ضبطها عند إضافة تصميم جديد.

---

## 8. مسارات واتساب والنماذج

### الرقم الموحّد

```js
var WA_NUMBER = "966556280382";   // في أعلى app.js — نقطة التغيير الوحيدة
```

### نافذة التحويل — `mt-wa.js`

**كل** انتقال إلى واتساب يمرّ عبر نافذة «جاري تحويلكم لرعاية خدمة العملاء»:
أيقونة واتساب نابضة داخل حلقة تفرغ تدريجيًا + عدّاد تنازلي 5 ثوانٍ + زر «التحويل الآن».

**الاستدعاء من أي مكان:**
```js
MTWA.go('https://wa.me/966556280382?text=...');
```

**تلقائيًا:** أي رابط في الصفحة يحتوي `wa.me` أو `api.whatsapp.com` يُعترض ويمرّ عبر النافذة — لا تحتاج ربطًا يدويًا للروابط الجديدة.

**لتغيير المدة:** عدّل `var SECONDS = 5;` أعلى `mt-wa.js`.

### الدالة الجسر في `app.js`

```js
function goWhatsApp(url, afterClose) { … }
```

تستدعي `MTWA.go()` إن كان محمّلًا، وإلا تفتح الرابط مباشرة (احتياط).

### النماذج الأربعة

| النموذج | المعرّف | المسار |
|---|---|---|
| استفسار عام | `inquiryModal` | تعبئة ← تأكيد ← نافذة التحويل ← واتساب |
| التحقق من الأهلية | `eligibilityModal` | معالج 6 خطوات ← تأكيد ← نافذة التحويل ← واتساب |
| طلب خاص | `customRequestModal` | تعبئة ← تأكيد ← نافذة التحويل ← واتساب |
| اختيار الخدمة | `chooserModal` | يظهر عند ضغط الزر العائم |

**بناء الرسالة:** كل نموذج له دالة `build…Message()` تُنتج نصًا عربيًا منسّقًا يُرسل في `?text=`.

### تباعد النماذج — في `mt-ui.css`

```css
.form-group { margin-bottom: 22px }                    /* بين الحقول */
.form-group input { padding: 14px 16px; font-size: .95rem }
.form-group textarea { min-height: 96px }
.modal-actions { margin-top: 26px; padding-top: 22px; border-top: … }
```

على الجوال حجم الخط **16 بكسل** إلزاميًا — يمنع تكبير iOS التلقائي عند الكتابة.

---

### ختم «متجر موثّق»

ختم التوثيق من **المركز السعودي للتنافسية والأعمال**، له **مربع مستقل** في
شبكة التذييل، موضعه **قبل** مربع «تحقق من بيانات المنشأة».

**ترتيب مربعات التذييل:**
```
بيانات المنشأة → متجر موثّق → تحقق من بيانات المنشأة → التسجيل الضريبي → خدمة العملاء
```

**العنصر في `index.html`:**
```html
<div class="footer-block sbc-block reveal">
  <h4>متجر موثّق</h4>
  <p class="sbc-desc">توثيق رسمي من المركز السعودي للتنافسية والأعمال — امسح الرمز…</p>
  <div class="sbc-seal-wrap">
    <div class="sbc-verify-seal" data-token="VGF6Qno2YURoZS9NOWJZZ2ZQM0djUT09"></div>
  </div>
</div>
```

**السكربت** قبل `</body>` مباشرة:
```html
<script src="https://eauthenticate.saudibusiness.gov.sa/EAuthSealApi/seal.js" async></script>
```

**التنسيق** في `mt-ui.css` قسم 5 — بألوان المتجر:
شريط علوي بتدرّج `--gold-light → --gold`، خلفية زرقاء شفافة، أيقونة درع
بتدرّج العلامة قبل العنوان، وحاوية بيضاء للختم نفسه.

**أحجام الختم حسب الشاشة:**

| الشاشة | أقصى ارتفاع للختم | ارتفاع الحاوية |
|---|---|---|
| ≥ 1440px | 132 بكسل | 140 |
| افتراضي | `clamp(88px, 11vw, 124px)` | `clamp(96px, 12vw, 132px)` |
| ≤ 900px | 110 بكسل | 118 |
| ≤ 768px | 116 بكسل | 124 |
| ≤ 480px | 104 بكسل | 112 |

> **ملاحظة:** التعليمات الرسمية تضع الختم **عائمًا** في زاوية الشاشة عبر
> `data-position="bottom-left"`. حذفنا هذه الخاصية وأضفنا قواعد تُثبّته داخل
> المربع، مع قاعدة احتياطية `body > .sbc-verify-seal` تُبعده عن زر واتساب
> العائم إن أصرّ السكربت على التعويم.

**الرمز `data-token` خاص بالمنشأة** — لا تغيّره إلا بتوكن جديد من المركز السعودي.

---

## 9. نظام الأيقونات

الأيقونات **SVG مضمّنة في `app.js`** — لا ملفات ولا مكتبة خارجية.

**الاستخدام:**
```html
<span class="icon-3d" data-icon="clock"></span>
<span class="icon-3d gold" data-icon="verify"></span>   <!-- بلون مميّز -->
```

الدالة `applyIcons()` تحوّلها إلى SVG عند تحميل الصفحة.

**الأيقونات المتاحة (33):**

```
home · business · company · star · message · vision · target · rocket
analytics · smartphone · laptop · tablet · gamepad · watch · headphones
accessory · desktop · network · magic · verify · mail · phone · clock
whatsapp · search · user · location · calendar · tag · palette · ruler
image · money
```

**لإضافة أيقونة:** أضف سطرًا في كائن `ICONS` في `app.js`:
```js
اسمها: '<path d="..."/>',
```
المسارات تُرسم على شبكة `24×24` بـ `stroke` لا `fill`.

---

## 10. الصور: التحويل والتسمية

### القاعدة الذهبية

**لا ترفع PNG أو JPG أبدًا.** كل صورة تمرّ عبر سكربت التحويل أولًا.
سبب: صورة PNG واحدة = 1 ميجابايت. نفسها WebP = 100 كيلوبايت. الفرق 90%.

### سكربت صور الفيديو

```bash
pip install pillow
python3 build/convert-video-images.py صورتك.png اسم-الصورة
```

**ماذا يفعل:**
- يضع الصورة كاملة داخل إطار 1920×1080 (16:9) **بلا اقتصاص**
- يملأ الفراغ الجانبي بنسخة مكبّرة ضبابية من الصورة نفسها
- يُخرج نسختين:
  - `اسم-الصورة.webp` — 1920×1080 للشاشات الكبيرة (أقل من 300 ك.ب)
  - `اسم-الصورة-m.webp` — 960×540 للجوال (أقل من 120 ك.ب)

**لمجلد كامل:**
```bash
python3 build/convert-video-images.py ./مجلد-الصور/
```

### سكربت شرائح السلايدر

```bash
python3 build/convert-banners.py صورتك.png a2
```
يُخرج `assets/slides/a2.webp` و `a2-m.webp`.

### قواعد التسمية

- **إنجليزية · حروف صغيرة · شرطة بين الكلمات** (kebab-case)
- وصفية لا رقمية: `iphone-17-pro` ✅ · `image-5` ❌
- مجموعة بالبادئة: `ipad-*` · `iphone-*` · `installment-*` · `*-grid`
- لاحقة `-m` محجوزة لنسخة الجوال — لا تستخدمها في اسم أصلي

### الحدود

| النوع | الحد الأقصى |
|---|---|
| صورة فيديو (سطح مكتب) | 300 كيلوبايت |
| صورة فيديو (جوال) | 120 كيلوبايت |
| المجموع الكلي للموقع | ابقَ تحت 20 ميجابايت |

---

## 11. النطاق والاستضافة

### الإعداد الحالي

| البند | القيمة |
|---|---|
| النطاق | `madaltashilsa.com` |
| المسجّل | سحابة — `my.sahabah.com.sa` |
| خوادم الأسماء | `ns10.hawsabah.com.sa` · `ns11.hawsabah.com.sa` |
| الاستضافة | GitHub Pages من فرع `main`، المجلد الجذر |
| الشهادة | Let's Encrypt — تُجدَّد تلقائيًا كل 3 أشهر |
| HTTPS | مُفعّل إجباريًا (Enforce HTTPS) |

### سجلات DNS — 9 سجلات

| النوع | الاسم | القيمة |
|---|---|---|
| A | @ | 185.199.108.153 |
| A | @ | 185.199.109.153 |
| A | @ | 185.199.110.153 |
| A | @ | 185.199.111.153 |
| AAAA | @ | 2606:50c0:8000::153 |
| AAAA | @ | 2606:50c0:8001::153 |
| AAAA | @ | 2606:50c0:8002::153 |
| AAAA | @ | 2606:50c0:8003::153 |
| CNAME | www | madaltashilsa-hash.github.io |

> **ملاحظة عملية:** لوحة سحابة **ترفض** صيغة IPv6 المختصرة. أدخلها بالصيغة الكاملة:
> `2606:50c0:8000:0000:0000:0000:0000:0153`
> ثم تعرضها اللوحة مختصرة بعد الحفظ.

### ملف CNAME

في جذر المستودع، سطر واحد:
```
madaltashilsa.com
```
**أنشأه GitHub تلقائيًا. حذفه يفصل النطاق فورًا.**

### لربط نطاق جديد مستقبلًا

1. في لوحة المسجّل: أضف السجلات التسعة أعلاه
2. في GitHub: `Settings ← Pages ← Custom domain` ← اكتب النطاق ← Save
3. انتظر ظهور **DNS check successful** (دقائق)
4. انتظر إصدار الشهادة (حتى ساعة)، ثم أشّر على **Enforce HTTPS**

---

## 12. كيف تنشر تعديلًا

### الطريقة الأولى — رفع عبر واجهة GitHub (الأسهل)

1. افتح `github.com/madaltashilsa-hash/madaltashil`
2. **Add file ← Upload files**
3. اسحب الملفات (أو محتويات مجلد كامل — GitHub يحفظ المسارات الفرعية)
4. اكتب وصفًا للتغيير
5. **Commit changes**

### الطريقة الثانية — تعديل ملف واحد مباشرة

1. افتح الملف على GitHub
2. اضغط أيقونة القلم ✏️
3. عدّل ← **Commit changes**

### الطريقة الثالثة — Git من سطر الأوامر

```bash
git clone https://github.com/madaltashilsa-hash/madaltashil.git
cd madaltashil
# عدّل ما تريد
git add -A
git commit -m "وصف التغيير"
git push origin main
```

### بعد النشر

GitHub Pages يعيد البناء تلقائيًا خلال **1–3 دقائق**.
تابع الحالة من تبويب **Actions** في المستودع.

**إن لم تظهر التغييرات:** اضغط `Ctrl+Shift+R` (تحديث قسري يتجاوز ذاكرة المتصفح).

---

## 13. قائمة فحص قبل النشر

- [ ] كل الصور مرّت عبر سكربت التحويل (لا PNG/JPG في `assets/`)
- [ ] أسماء الصور kebab-case وصفية، وكل صورة لها نسخة `-m`
- [ ] لا تتكرر صورة بين فيديو وآخر
- [ ] لا يوجد تصميمان من نفس العائلة متتاليان في أي فيديو
- [ ] فتحت الصفحة على عرض **390 بكسل** (جوال) و **1440 بكسل** (سطح مكتب)
- [ ] النص في الفيديو أسفل الصورة ولا يغطّيها
- [ ] لوحة المطوّر `F12 ← Console` نظيفة بلا أخطاء
- [ ] تبويب `Network` بلا أخطاء 404
- [ ] جرّبت مسار واتساب كاملًا: الزر العائم ← استفسار ← إرسال ← ظهرت نافذة العدّاد
- [ ] الروابط الداخلية تعمل (القائمة الجانبية · الصفحات الفرعية)
- [ ] `CNAME` ما زال موجودًا في الجذر
- [ ] حدّثت `PROJECT-GUIDE.md` بما تغيّر

---

## 14. حل المشكلات الشائعة

### الموقع لا يفتح بعد ربط نطاق جديد

**السبب الغالب:** ذاكرة DNS المخزّنة على جهازك، لا الموقع.

**الفحص:** جرّب من بيانات الجوال (أوقف الواي-فاي). إن فتح، فالمشكلة محلية.

**الحل:**
```
chrome://net-internals/#dns      ← Clear host cache
chrome://net-internals/#sockets  ← Flush socket pools
```
ثم أغلق المتصفح **بالكامل** وأعد فتحه. أو أعد تشغيل الراوتر.

على ويندوز: `ipconfig /flushdns`
على ماك: `sudo dscacheutil -flushcache; sudo killall -HUP mDNSResponder`

### تحذير «الاتصال غير آمن» رغم أن الشهادة سليمة

**السبب:** زرت الموقع قبل إصدار الشهادة، فخزّن المتصفح الخطأ.

**الحل:**
```
chrome://net-internals/#hsts
```
قسم **Delete domain security policies** ← اكتب النطاق ← **Delete**.
ثم أغلق المتصفح بالكامل.

**تحقّق أولًا:** إن فتح الموقع على الجوال بلا تحذير، فالشهادة سليمة والمشكلة في متصفحك فقط.

### التعديلات لا تظهر بعد الرفع

1. تأكد من نجاح البناء: تبويب **Actions** في المستودع
2. `Ctrl+Shift+R` تحديث قسري
3. جرّب نافذة تصفّح خفي

### صورة لا تظهر

- تأكد أن **النسختين** موجودتان: `الاسم.webp` و `الاسم-m.webp`
- تأكد أن الاسم في `mt-video.js` يطابق اسم الملف **حرفًا بحرف**
- الأسماء حسّاسة لحالة الأحرف على GitHub Pages

### الفيديو لا يعمل

- تحقّق من الكونسول — خطأ JS واحد يوقف المشغّل كله
- تأكد أن `data-mtv` في HTML يطابق مفتاحًا في `VIDEOS`
- تأكد من تحميل `mt-video.css` و `mt-video.js`

---

## 15. قواعد ومحاذير

### افعل ✅

- **اقرأ هذا الملف قبل أي عمل** وحدّثه بعده
- حوّل كل صورة عبر السكربت قبل رفعها
- اختبر على الجوال وسطح المكتب قبل النشر
- افحص الكونسول بعد كل تعديل
- استخدم `var` لا `let/const` — الكود ES5 بالكامل
- ضع التحسينات الجديدة في `mt-ui.css` بدل تعديل `style.css`

### لا تفعل ❌

- **لا تحذف `CNAME`** — يفصل النطاق فورًا
- **لا تغيّر ترتيب تحميل ملفات CSS** — `mt-ui.css` يجب أن يبقى الأخير
- **لا تعيد تسمية `--gold`** رغم أن قيمته أزرق — مستخدم في عشرات المواضع
- **لا ترفع صورًا غير محوّلة** — تثقل الموقع بلا داعٍ
- **لا تضف مكتبات خارجية** — الموقع خفيف عمدًا
- **لا تعدّل موضع قسم الأهلية في `app.js`** — انقل `elig-anchor` في HTML بدلًا من ذلك
- **لا تضع صورتين من نفس عائلة التصميم متتاليتين** في الفيديو
- **لا تستخدم لاحقة `-m`** في أسماء الصور الأصلية

### أخطاء وقعت سابقًا — تجنّبها

| الخطأ | الأثر | الوقاية |
|---|---|---|
| صورة بشعار **AT** بدل **MT** | حُذفت الصورة بعد إنتاجها | افحص الشعار في كل صورة مولّدة |
| نص الفيديو فوق الصورة | يغطّي المحتوى | النص دائمًا في شريط مستقل أسفلها |
| 6 بطاقات مواصفات متتالية | يبدو تكرارًا للزائر | تناوب بين عائلات التصميم |
| IPv6 مختصر في لوحة سحابة | رفض الحفظ | استخدم الصيغة الكاملة |
| مرجع `intro-video` بعد حذف الملف | مرجع ميت في `app.js` | ابحث عن المراجع قبل حذف أي ملف |

---

## 16. بيانات ثابتة

### التواصل

| البند | القيمة |
|---|---|
| الجوال / واتساب | `0556280382` — دوليًا `966556280382` |
| البريد | `support@madaltashil.com` |

### أوقات العمل

| الفترة | الأيام | الوقت |
|---|---|---|
| صباحية | الأحد – الخميس | 9:00 ص – 12:00 ظ |
| مسائية | الأحد – الخميس | 4:00 ع – 12:00 م |
| السبت | مسائية فقط | 4:00 ع – 12:00 م |
| الجمعة | — | إجازة رسمية |

### العنوان الوطني

| البند | القيمة |
|---|---|
| الدولة | المملكة العربية السعودية |
| المدينة | حائل |
| الحي | حي السلام |
| الشارع | الملك سلمان بن عبدالعزيز |
| رقم المبنى | 7116 |
| الرقم الفرعي | 3078 |

### أرقام الخدمة المذكورة في الموقع

- التقسيط يصل إلى **60 شهرًا**
- الأقساط تبدأ من **150 ريالًا**
- التقييم على خرائط Google: **4.7** بناءً على 29 تقييمًا

---

## ملحق: أين أعدّل ماذا — جدول سريع

| أريد أن أغيّر… | الملف | الموضع |
|---|---|---|
| رقم واتساب | `app.js` | `var WA_NUMBER` — السطر 4 |
| مدة عدّاد التحويل | `mt-wa.js` | `var SECONDS` |
| مدة مشهد الفيديو | `mt-video.js` | `SCENE_MS` / `LOGO_MS` |
| نصوص الفيديو أو صوره | `mt-video.js` | كائن `VIDEOS` |
| سرعة السلايدر | `hero-slider.js` | `AUTOPLAY` |
| شرائح السلايدر | `hero-slider.js` | كائن `GROUPS` |
| ألوان الموقع | `style.css` | `:root` — السطر 19 |
| ألوان الفيديو | `mt-video.css` | `.mtv-section` |
| تباعد النماذج | `mt-ui.css` | قسم «الاستمارات» |
| مسافات الأقسام | `mt-ui.css` | قسم «مسافات الأقسام» |
| القائمة الجانبية | `mt-ui.css` | قسم «القائمة الجانبية» |
| ترتيب أقسام الصفحة | `index.html` | داخل `<main>` |
| موضع قسم الأهلية | `index.html` | انقل `<div id="elig-anchor">` |
| أوقات العمل | `index.html` | `.sidebar-hours` و `.hours-footer` |
| الأسئلة الشائعة | `index.html` + `faq.html` | `.faq-grid` |
| أيقونة جديدة | `app.js` | كائن `ICONS` |

---

*نهاية الدليل — حدّثه كلما تغيّر شيء، فهو مرجعك ومرجع من يأتي بعدك.*
